[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "demo"
version = "0.1.0"
requires-python = ">=3.12"

[tool.moltark]
schema-version = 1
template-version = "python/v1"
